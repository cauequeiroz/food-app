import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginVertical: 20,
  },
  icon: {
    fontSize: 64,
    textAlign: 'center',
    width: 100,
    marginLeft: 15
  }
});